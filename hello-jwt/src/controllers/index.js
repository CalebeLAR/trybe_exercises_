const ping = require('./ping');
const { login } = require('./login');
const { userMe } = require('./users');
const { topSecret } = require('./topSecret');


module.exports = {
  ping,
  login,
  userMe,
  topSecret,
};
